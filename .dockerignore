.venv/
venv/
env/
__pycache__/
*.py[cod]
.pytest_cache/
.cursor/
.vscode/
*.code-workspace
.DS_Store
Thumbs.db

# Don't send the frontend source to Docker — Dockerfile copies it via COPY
# But we DO want the frontend/ directory (the Dockerfile stages it)
# So exclude only heavy artifacts, not the source:
frontend/node_modules/
frontend/dist/

# Dev-only files
*.log
.env.local

# Test artifacts  
tests/__pycache__/
