[project]
name = "diagnostiq"
version = "0.1.0"
description = "Diagnostiq — multimodal technical-support agent for the Trane Precedent RTU"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pytest>=9.0.3",
    "sentence-transformers>=5.4.0",
]
